<div class='nav'>
  <div>
    <ul>
      <% first = true %>
      <% @pages.each do |p| %>
        <li onclick="show_page(this, '<%=p.name.downcase%>');" class="<%=(first ? 'active' : '')%>"><%= content_tag :span, p.name, :class=>(first ? 'active' : '') %></li>
        <% first = false %>
      <% end %>
    </ul>
  </div>
</div>